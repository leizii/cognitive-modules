{
  "$validate": {
    "simplified_code": {
      "type": "string",
      "minLength": 1
    },
    "changes": {
      "type": "array"
    },
    "behavior_equivalence": {
      "type": "boolean"
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    }
  },
  "$example": {
    "simplified_code": "function processData(data) {\n  return data.filter(item => item != null).map(item => item * 2);\n}",
    "changes": [
      {
        "type": "simplify_logic",
        "description": "Replaced for loop with filter and map",
        "scope": "function",
        "risk": "low"
      },
      {
        "type": "remove_redundancy",
        "description": "Used != null to check both null and undefined",
        "scope": "local",
        "risk": "none"
      }
    ],
    "behavior_equivalence": true,
    "complexity_reduction": 50,
    "summary": "Replaced imperative loop with functional filter/map chain",
    "rationale": "The for loop with push can be replaced with filter().map() which is more idiomatic JavaScript. Using != null is equivalent to checking both null and undefined.",
    "confidence": 0.9
  }
}
